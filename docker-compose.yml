version: '3.8'

services:
  genai-style-app:
    build: .
    container_name: genai-style-app
    ports:
      - "30123:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - web

networks:
  web:
    driver: bridge